1. Clone the project into a folder
2. We should have node.js and python3 install in our system
3. run command " npm install "
4. run command " pip install Flask"
5. run command " npm run dev" in one tab and run " python3 flask-api/server.py " in another tab
6. visit localhost:3000 to see the app running
 